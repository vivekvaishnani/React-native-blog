import fs from 'fs-extra';

import { AndroidKeystore } from '../baseJob';

async function saveKeystore(keystore: AndroidKeystore, keystorePath: string) {
  await fs.writeFile(
    keystorePath,
    Buffer.from(keystore.keystoreDataBase64, 'base64'),
  );
}

export { saveKeystore };
