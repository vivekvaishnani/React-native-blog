declare module 'envsub' {
  interface EnvDefinition {
    name: string;
    value?: string;
  }

  interface EnvsubOptions {
    templateFile: string;
    outputFile: string;
    options?: {
      diff?: boolean = false;
      protect?: boolean = false;
      syntax?: string;
      envs: EnvDefinition[];
    } = {};
  }

  interface EnvsubResult {
    templateFile: string;
    templateContents: string;
    outputFile: string;
    outputContents: string;
  }

  function envsub(options: EnvsubOptions): Promise<EnvsubResult>;

  export = envsub;
}
