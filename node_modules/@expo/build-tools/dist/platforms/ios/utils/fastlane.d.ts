import bunyan from 'bunyan';
interface FastlaneOptions {
    logger?: bunyan;
    envs?: {
        [key: string]: string;
    };
    cwd?: string;
}
declare function runFastlane(fastlaneArgs: string[], { logger, envs, cwd }?: FastlaneOptions): Promise<import("@expo/spawn-async").SpawnResult>;
export default runFastlane;
