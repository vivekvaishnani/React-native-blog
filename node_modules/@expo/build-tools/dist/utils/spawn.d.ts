import { SpawnResult, SpawnOptions } from '@expo/spawn-async';
import bunyan from 'bunyan';
declare function spawn(command: string, args: string[], options?: SpawnOptions & {
    logger?: bunyan;
    stdoutOnly?: boolean;
}): Promise<SpawnResult>;
export default spawn;
