import { AnyConfiguration, InputEnvironment } from '../types';
export default function withEntry(webpackConfig: AnyConfiguration, env: InputEnvironment | undefined, options: {
    entryPath: string;
    strict?: boolean;
}): AnyConfiguration;
